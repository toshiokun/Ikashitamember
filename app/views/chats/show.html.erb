<p id="notice"><%= notice %></p>

<p>
  <strong>Message:</strong>
  <%= @chat.message %>
</p>

<p>
  <strong>User:</strong>
  <%= @chat.user.name %>
</p>

<p>
  <strong>Spoken user:</strong>
  <%= User.find_by(id: @chat.spoken_user_id).name %>
</p>
<% if @chat.user_id == current_user.id %>
<%= link_to 'Edit', edit_chat_path(@chat) %> |
<% end %>
<%= link_to 'Back', chats_path %>
